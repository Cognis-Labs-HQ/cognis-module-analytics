{
  "name": "@cognis/module-analytics",
  "version": "2.1.0",
  "private": false,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check:manifest": "node scripts/validate-manifest.js",
    "manifest:hashes": "node scripts/update-manifest-hashes.js",
    "check": "npm test && npm run check:manifest"
  }
}
